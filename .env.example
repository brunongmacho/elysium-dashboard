# ============================================================================
# ELYSIUM DASHBOARD - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env.local and fill in your values
# ============================================================================

# ----------------------------------------------------------------------------
# MongoDB Atlas Configuration
# ----------------------------------------------------------------------------
# Your MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
# Database name should be "elysium-bot" (same as the Discord bot)
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/elysium-bot?retryWrites=true&w=majority
MONGODB_DB_NAME=elysium-bot

# ----------------------------------------------------------------------------
# Discord OAuth2 Configuration (Required for Phase 3)
# ----------------------------------------------------------------------------
# Discord OAuth2 Application Credentials
# Create at: https://discord.com/developers/applications
# - Create new application
# - Go to OAuth2 section
# - Add redirect URL: http://localhost:3000/api/auth/callback/discord
# - For production: https://your-domain.vercel.app/api/auth/callback/discord
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret

# Your ELYSIUM guild (server) ID
# How to get: Enable Developer Mode in Discord → Right-click server → Copy ID
DISCORD_GUILD_ID=your_guild_id

# ELYSIUM role ID (members with this role can mark bosses as killed)
# How to get: Enable Developer Mode → Right-click role → Copy ID
DISCORD_ELYSIUM_ROLE_ID=your_elysium_role_id

# Admin role ID (for future admin-only features)
# Optional: If not set, only ELYSIUM role will be checked
# Can be comma-separated for multiple admin roles: role1,role2,role3
DISCORD_ADMIN_ROLE_ID=your_admin_role_id

# Role badges for member profiles (all optional)
DISCORD_LEADER_ROLE_ID=your_leader_role_id
DISCORD_VICE_LEADER_ROLE_ID=your_vice_leader_role_id
DISCORD_CORE_ROLE_ID=your_core_role_id

# ----------------------------------------------------------------------------
# NextAuth Configuration
# ----------------------------------------------------------------------------
# NextAuth secret for session encryption
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_random_secret_key_here

# Application URL
# Development: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXTAUTH_URL=http://localhost:3000

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------
# Default timezone for boss timers (GMT+8 for Manila time)
NEXT_PUBLIC_TIMEZONE=Asia/Manila

# Enable debug logging (true/false)
NEXT_PUBLIC_DEBUG=false

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Copy this file:
#    cp .env.example .env.local
#
# 2. Fill in MongoDB credentials (REQUIRED for Phase 1):
#    - Get connection string from MongoDB Atlas
#    - Ensure it points to your "elysium-bot" database
#
# 3. Discord OAuth2 (REQUIRED for Phase 3):
#    - Create Discord OAuth2 app at https://discord.com/developers/applications
#    - Add redirect URLs for local and production
#    - Copy Client ID and Client Secret
#    - Get your guild ID and role IDs from Discord
#
# 4. Generate NextAuth secret:
#    openssl rand -base64 32
#
# 5. Test connection:
#    npm run dev
#    Visit http://localhost:3000
#
# ============================================================================
